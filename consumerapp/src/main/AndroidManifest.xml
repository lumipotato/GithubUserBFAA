<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lumi.consumerapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="com.lumi.submission2.READ_DATABASE"/>
    <uses-permission android:name="com.lumi.submission2.WRITE_DATABASE"/>

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design">
        <activity
            android:name=".ui.main.Detail"
            android:parentActivityName=".ui.main.FavoriteActivity" />
        <activity android:name=".ui.main.FavoriteActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>