<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lumi.submission2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission
        android:name="com.lumi.submission2.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.lumi.submission2.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver
            android:name=".misc.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <provider
            android:name=".db.UserProvider"
            android:authorities="com.lumi.submission2"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.lumi.submission2.READ_DATABASE"
            android:writePermission="com.lumi.submission2.WRITE_DATABASE" />

        <activity android:name=".ui.setting.SettingHolder" />
        <activity android:name=".ui.main.FavoriteActivity" />
        <activity
            android:name=".ui.main.DetailActivity"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity android:name=".ui.main.MainActivity"/>
            <activity android:name=".ui.main.SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>